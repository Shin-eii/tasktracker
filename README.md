# TaskTracker CLI

A command-line task manager built with Python.

## Usage

```
python tasks.py add <title>
python tasks.py list
python tasks.py done <id>
python tasks.py delete <id>
python tasks.py search <keyword>
```
